# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# secrets / local env (never commit keys)
.env
*.env.local
configs/*.env
!configs/*.env.example

# run artifacts / data (huge; lives on the project FS, never committed)
runs/
workspaces/
logs/
data/
mlebench-data/
mle-bench/
*.csv
!tests/**/*.csv
# full-run trace bundles (run.json, llm_calls.jsonl, artifacts/) live under runs/
llm_calls.jsonl
run.json
# downloader logs/progress from data prep
*.progress
*.nohup
prepare_all.log
dl_*.log

# tooling
.pytest_cache/
.uv-cache/
.DS_Store
